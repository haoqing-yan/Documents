# 银行核心系统架构

银行核心架构的目标不是追求“技术最新”，而是在 **一致性、可用性、性能、可审计、可恢复、可演进** 之间取得平衡。

## 1. 集中式核心

传统核心常见特点：

- 应用集中部署；
- 数据库集中；
- 事务边界清晰；
- 强依赖关系型数据库；
- 批量与联机统一协调。

优势：

- 一致性处理简单；
- 账务边界清楚；
- 运维模型成熟。

不足：

- 扩展成本高；
- 发布影响面大；
- 大表和热点账户问题明显；
- 模块耦合容易越来越重。

## 2. 集群

集群解决的是单实例容量和高可用问题。

常见要求：

- 应用尽量无状态；
- Session 外置；
- 配置集中；
- 服务健康检查；
- 请求可在节点间切换。

如果应用有本地状态，节点切换后就可能出现数据不一致或交易状态丢失。

## 3. 分布式

分布式并不是把一个系统拆成很多服务就结束了。

真正的难点包括：

- 分布式事务；
- 数据一致性；
- 服务治理；
- 链路追踪；
- 超时；
- 重试；
- 幂等；
- 顺序性；
- 故障隔离。

### 数据库分布

常见形式：

```text
路由层 -> 逻辑库 -> 物理分片
```

分片键可能是：

- 客户号；
- 账号；
- 法人号；
- 机构号。

设计分片键时必须考虑：

- 单账户交易应尽量落在一个分片；
- 跨账户交易是否跨分片；
- 热点客户是否集中；
- 数据迁移是否方便。

## 4. 微服务

微服务适用于业务边界明确、独立演进的模块，但不能机械拆分核心账务。

比较适合拆分的能力：

- 客户；
- 产品；
- 参数；
- 通知；
- 查询；
- 非账务外围能力。

对于余额、账务、账户状态等强一致链路，需要谨慎定义边界。

## 5. 有状态服务与无状态服务

### 无状态服务

请求需要的数据来自数据库、缓存或外部存储，任何实例均可处理。

优点：

- 容易扩容；
- 容易故障切换；
- 适合容器化。

### 有状态服务

本地持有交易上下文、会话、任务执行状态等。

需要额外解决：

- 状态迁移；
- 故障恢复；
- 主备切换；
- 重复执行。

银行联机服务通常尽可能设计为无状态。

## 6. 分布式事务

不建议把所有跨系统交易都强行做成全局 ACID。

常见策略：

### 本地事务 + 可靠事件

```text
本地业务数据
+ 本地事件表
在同一数据库事务提交
```

之后异步发送消息，失败可重试。

### TCC

Try / Confirm / Cancel。

适合需要明确资源预留的场景，但实现复杂。

### Saga

一系列本地事务 + 补偿事务。

适合长事务和跨服务业务流程。

### 最终一致性

对于不影响核心资金正确性的外围信息，可以允许延迟一致。

例如：

- 通知；
- 标签；
- 营销数据；
- 数据仓库同步。

## 7. 缓存

缓存常用于：

- 产品参数；
- 机构信息；
- 字典；
- 热点查询。

不建议把缓存当作核心余额唯一数据源。

必须处理：

- 缓存失效；
- 双写一致性；
- 穿透；
- 击穿；
- 雪崩；
- 热点 key。

## 8. 数据同步

同城双中心、异地灾备和数据平台都涉及复制。

需要明确：

- 同步复制还是异步复制；
- RPO；
- RTO；
- 网络中断如何处理；
- 主备切换后如何防止双主写入；
- 数据回切如何处理。

## 9. ESB / API Gateway

ESB 常见能力：

- 协议转换；
- 服务路由；
- 编排；
- 报文转换。

API Gateway 更偏：

- API 管理；
- 认证鉴权；
- 限流；
- 灰度；
- 开放平台接入。

不要让网关承担核心业务状态。

## 10. 消息队列

银行系统常用 MQ 解耦：

- 会计通知；
- 短信通知；
- 数据同步；
- 对账；
- 批处理任务。

设计时必须回答：

- 消息会不会重复？
- 消费者是否幂等？
- 消息丢失如何补发？
- 是否要求顺序？
- 死信如何处理？

通常应按“消息至少可能被投递两次”来设计消费者。

## 11. 可观测性

银行系统必须能根据一笔流水快速定位全链路。

至少应具备：

- traceId；
- transactionId；
- channelSeq；
- coreSeq；
- 结构化日志；
- 指标监控；
- 链路追踪；
- 告警。

日志中不得随意记录：

- 完整身份证；
- 完整银行卡号；
- 密码；
- PIN；
- CVV；
- 密钥。

## 12. 高可用设计

典型策略：

- 应用多实例；
- 数据库主备；
- MQ 集群；
- 注册中心集群；
- 限流；
- 熔断；
- 降级；
- 隔离；
- 容灾切换。

核心理念：**任何单点故障都不应直接导致银行整体交易不可用。**

## 13. 两地三中心

```text
生产中心
   |
同城灾备中心
   |
异地灾备中心
```

常见目标：

- 同城：处理机房级故障，低延迟切换；
- 异地：处理城市级灾害和重大事故。

具体建设模式与监管、银行规模和成本有关。

## 14. 架构设计检查清单

- 强一致边界在哪里？
- 哪些数据允许最终一致？
- 服务调用超时后怎么办？
- 重试是否会造成重复交易？
- 跨库交易如何处理？
- MQ 重复消费是否安全？
- 单机房不可用能否切换？
- 是否存在单点？
- 是否能通过流水定位完整链路？
- 数据库恢复后如何做账务勾稽？
