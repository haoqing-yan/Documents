# 银行批量、日终与对账

银行核心不仅有白天的联机交易，还有大量依赖业务日期推进的批处理。

很多“账户白天看起来正常、第二天账不平”的问题，都出在日终批量和对账。

---

# 1. 日切与日终不是一回事

## 日切

把核心业务日期从：

```text
T
```

切换为：

```text
T + 1
```

## 日终

围绕 T 日完成：

- 计息；
- 计提；
- 结息；
- 费用；
- 逾期迁移；
- 清算；
- 总账；
- 对账；
- 报表。

有些架构会先切业务日再继续跑 T 日批量，因此联机和批量可能并行。

---

# 2. 业务日期中心

不要让每个模块直接调用：

```java
LocalDate.now()
```

核心应有统一 BusinessDateService：

```text
system_date
business_date
previous_business_date
next_business_date
batch_status
```

交易必须明确自己使用哪一个日期。

---

# 3. 批次模型

建议至少有：

```text
batch_run
batch_job
batch_step
batch_item
```

## batch_run

表示某业务日的一次总批次。

## batch_job

例如：

```text
INTEREST_ACCRUAL
LOAN_OVERDUE
TERM_DEPOSIT_MATURITY
GENERAL_LEDGER
RECONCILIATION
```

## batch_step

一个任务再拆步骤。

## batch_item

必要时记录账户/借据级处理状态。

---

# 4. 批任务状态机

```text
WAITING
↓
RUNNING
├─ SUCCESS
├─ PARTIAL_SUCCESS
└─ FAILED
```

重试时不能只把 FAILED 改回 RUNNING，而应明确：

- 全量重跑；
- 从断点续跑；
- 仅失败项重跑。

---

# 5. 批量任务必须幂等

例如贷款逾期迁移：

第一次：

```text
正常本金 1000 -> 逾期本金 1000
```

如果任务重跑不能再次：

```text
逾期本金 +1000
```

所以幂等键可以是：

```text
business_date
+ loan_id
+ batch_event_type
```

---

# 6. 常见日终任务顺序

不同银行不同，但可以理解为存在依赖图：

```text
停止/切换部分业务
↓
日切
↓
账户日终准备
↓
利息积数
↓
计提
↓
结息/到期
↓
贷款扣款
↓
逾期迁移
↓
费用处理
↓
支付清算
↓
会计汇总
↓
总分核对
↓
外部对账
↓
报表/数据抽取
↓
日初初始化
```

不要简单认为所有 job 可以随意并行。

---

# 7. 任务依赖 DAG

推荐明确任务依赖：

```text
A -> B -> D
 \-> C -> D
```

例如：

```text
贷款计提
  ↓
贷款总账
  ↓
总分核对
```

如果上游未成功，下游不应该继续产生错误结果。

---

# 8. 分片处理

账户量很大时，批任务通常分片：

```text
hash(account_id) % N
```

或者按：

- 机构；
- 法人；
- 账户区间；
- 客户区间。

目标：并行提升吞吐。

但同一业务对象不要被多个分片重复处理。

---

# 9. 断点续跑

批任务可能运行数小时。

应记录 checkpoint：

```text
last_processed_key
processed_count
success_count
failed_count
```

故障恢复后可以从断点继续。

---

# 10. 联机与批量并发

日终期间联机仍可能进行。

要考虑：

```text
联机修改余额
同时
批量计算积数/利息
```

常见解决方向：

- 业务日期隔离；
- 快照；
- 账户最后处理日期；
- 版本控制；
- 分区批量。

不能简单依赖“晚上没人交易”。

---

# 11. 总分核对

核心检查：

```text
总账余额
=
分户账余额汇总
```

按至少这些维度：

```text
法人
机构
币种
科目
业务日期
```

发现差异时必须输出差异明细，而不是只报一个“不平”。

---

# 12. 账户余额与流水核对

校验：

```text
期初余额
+ 当日借贷发生额
= 期末余额
```

用于发现：

- 漏流水；
- 重复流水；
- 余额直接被修改；
- 批量重复处理。

---

# 13. 系统间对账

例如：

```text
核心 vs 支付
核心 vs 信贷
核心 vs 卡系统
核心 vs 清算平台
```

对账维度通常包括：

```text
流水号
金额
币种
状态
日期
```

---

# 14. 对账结果分类

常见：

```text
双方有且一致
本方有，对方无
本方无，对方有
双方都有但金额不同
双方都有但状态不同
```

其中后四种进入差错处理。

---

# 15. 单边账

例如：

```text
核心：成功
支付：查无此笔
```

或者：

```text
核心：失败
支付：成功
```

这是资金系统中必须重点处理的异常。

不能通过人工直接改状态解决，需要确认资金真实位置。

---

# 16. 自动差错处理

对账发现差异后可以自动：

```text
查询原交易
↓
重新确认状态
↓
补记账 / 冲正 / 退款
```

只有无法自动确认的异常才进入人工差错平台。

---

# 17. 调账

调账属于高风险能力。

标准流程：

```text
提交调账申请
↓
业务审核
↓
会计复核
↓
生成调账凭证
↓
账务引擎记账
↓
审计
```

严禁：

```sql
update account set balance = ...
```

---

# 18. 挂账清理

挂账户需要每日监控：

```text
挂账金额
挂账笔数
账龄
来源系统
责任部门
```

长期未销账是很重要的运营风险信号。

---

# 19. 日初处理

新业务日开始可能执行：

- 新产品参数生效；
- 新利率生效；
- 新限额生效；
- 节假日参数刷新；
- 批次状态初始化；
- 日初报表。

---

# 20. 批量平台技术能力

建议支持：

```text
任务编排
依赖管理
分片
并行
断点续跑
幂等
失败重试
人工跳过
人工重跑
超时告警
运行日志
指标监控
```

---

# 21. 批量监控指标

至少：

```text
任务耗时
处理笔数
成功数
失败数
TPS
等待时间
重试次数
数据积压
```

并对比历史基线。

例如平时 20 分钟的任务突然运行 2 小时，即使没有报错也需要告警。

---

# 22. 日终最危险的异常

1. 重复跑批导致重复记账；
2. 一半账户处理成功、一半失败；
3. 业务日期已切换但旧日批次未完成；
4. 总账已汇总，业务账又发生补记；
5. 外部清算文件缺失；
6. 对账结果未处理就继续下一日；
7. 手工跳过任务但没有补偿。

---

# 23. 日终检查清单

每个批任务必须明确：

- 输入业务日期；
- 上游依赖；
- 输出数据；
- 幂等键；
- 分片方式；
- 重跑方式；
- checkpoint；
- 会计影响；
- 失败补偿；
- 是否允许跳过；
- 对账方式；
- 告警阈值。

银行批量系统真正的目标不是“每天能跑完”，而是：

> **任何一个任务失败、重复、延迟或重跑时，都能明确知道钱和账现在处于什么状态。**
