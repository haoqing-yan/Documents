# 银行限额、风控与差错处理

银行交易不能只有“业务校验通过/失败”。真正生产系统还要处理限额、风险拦截、异常状态、单边账和人工差错运营。

---

# 1. 三类控制不要混淆

```text
技术限流   控制 QPS / 并发
业务限额   控制金额 / 次数
风险控制   判断交易是否异常或高风险
```

例如：

```text
每秒 100 次       -> 技术限流
单日转账 5 万     -> 业务限额
异地设备大额转账   -> 风险规则
```

---

# 2. 限额维度

常见：

```text
客户
账户
产品
渠道
交易类型
收款人
商户
机构
币种
```

时间维度：

```text
单笔
日累计
月累计
自然日
业务日
滚动 24 小时
```

---

# 3. 限额模型

```text
LimitDefinition
LimitInstance
LimitUsage
LimitReservation
```

## 定义

例如：

```text
手机银行个人转账日限额 5 万
```

## 实例

客户 A 可能因认证等级不同调整为 10 万。

## 使用量

```text
used_amount
used_count
```

## 预占

对于异步交易可以先占用限额，失败后释放。

---

# 4. 并发限额问题

客户日剩余额度 1 万，同时发起两笔 8000。

如果：

```text
读剩余额度
-> 判断
-> 再更新
```

两个请求可能都通过。

因此限额使用量更新需要：

- 原子更新；
- 乐观锁；
- 悲观锁；
- 串行化；
- Redis Lua 等具备原子性的实现。

最终真源和恢复机制必须明确。

---

# 5. 限额预占

异步支付可采用：

```text
检查额度
↓
预占额度
↓
支付处理中
├─ 成功 -> 预占转正式使用
└─ 失败 -> 释放
```

与授信额度冻结思想一致。

---

# 6. 实时风险规则

可能包括：

```text
异常设备
异常 IP
异地登录
短时间高频
金额突增
陌生收款人
高风险商户
黑名单
账户接管特征
```

风险系统通常输出：

```text
PASS
REJECT
REVIEW
CHALLENGE
```

其中 CHALLENGE 可触发额外认证。

---

# 7. 风险与核心边界

风控系统负责做风险决策，但核心仍应维护最基本的强规则：

- 账户状态；
- 余额；
- 产品限制；
- 强制冻结；
- 法定控制。

不能把所有关键限制都依赖一个可能超时的外围风控服务。

---

# 8. 风控超时

必须提前定义：

```text
风控不可用时
是 fail-open
还是 fail-close
```

资金类高风险交易通常不能默认无条件放行。

具体策略应按业务风险等级设计。

---

# 9. 差错类型

典型：

```text
状态不一致
金额不一致
重复记账
漏记账
单边账
清算差异
余额与流水不一致
总分账不平
```

每类差错需要不同处置方式。

---

# 10. 单边账

例如：

```text
客户账户已扣款
但支付订单不存在成功结果
```

或：

```text
外部支付成功
核心账户未入账
```

处理前必须先确认真实资金位置。

---

# 11. 差错平台

推荐实体：

```text
ExceptionCase
ExceptionType
SourceTransaction
ReconciliationResult
HandlingAction
ApprovalRecord
```

一个差错 case 应能关联所有原始流水。

---

# 12. 自动处理优先

典型流程：

```text
发现异常
↓
自动查询原交易
↓
自动查询外部状态
↓
规则判断
├─ 自动冲正
├─ 自动补账
├─ 自动退款
└─ 转人工
```

不要把所有差错都丢给运营人工处理。

---

# 13. 人工处理

人工操作至少采用双人或多级控制：

```text
经办
↓
复核
↓
授权
↓
执行
```

并记录：

- 原因；
- 证据；
- 金额；
- 原交易；
- 操作人；
- 审核人。

---

# 14. 补账

如果业务真实成功但本行漏记，可以生成补账交易。

补账不能直接补一条余额，而应走记账引擎：

```text
补账凭证
↓
会计规则
↓
余额
↓
明细
↓
总账
```

---

# 15. 调账

调账用于会计纠错，通常权限高于普通补单。

必须：

```text
有明确借贷双方
借贷平衡
完整审批
可追溯
```

---

# 16. 挂账

状态暂时无法解决的资金可以进入挂账户。

每条挂账应有：

```text
source_transaction
amount
currency
reason
owner
created_date
age
status
```

必须持续清理，而不是把挂账当垃圾桶。

---

# 17. 账龄管理

可以按：

```text
0~1 天
2~3 天
4~7 天
8~30 天
30 天以上
```

统计未处理差错和挂账。

账龄越长风险越高。

---

# 18. 异常处理状态机

```text
OPEN
↓
INVESTIGATING
↓
ACTION_REQUIRED
↓
PROCESSING
↓
RESOLVED
↓
CLOSED
```

必要时还可有：

```text
REOPENED
```

---

# 19. 推荐服务拆分

```text
LimitService
LimitUsageService
RiskDecisionAdapter
ExceptionDetectionService
ExceptionCaseService
ReconciliationService
CompensationService
AdjustmentService
SuspenseService
```

---

# 20. 核心检查清单

## 限额

- 限额按哪些维度？
- 使用业务日还是自然日？
- 并发是否会穿透？
- 异步交易是否预占？
- 失败是否释放？

## 风控

- 风控超时策略是什么？
- REVIEW 如何进入人工？
- 核心自身有哪些强制控制？

## 差错

- 如何发现单边账？
- 自动补偿规则是什么？
- 哪些必须人工审批？
- 挂账如何跟踪？
- 调账是否经过 Posting Engine？
- 是否有完整审计链？
