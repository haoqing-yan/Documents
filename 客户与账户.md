# 客户与账户

客户与账户是银行核心最基础的两个对象，但二者不能混为一谈。

## 1. 客户（CIF）

客户信息系统通常为客户生成唯一客户号（CIF No.）。

一个客户可以拥有多个账户，一个账户一般属于某个客户或某个组织主体。

个人客户常见信息：

- 客户号；
- 姓名；
- 证件类型与证件号；
- 联系方式；
- 客户状态；
- KYC 状态；
- 风险等级；
- 税收居民信息；
- 职业与地址。

对公客户还可能包含：

- 企业名称；
- 统一社会信用代码；
- 法定代表人；
- 实际控制人；
- 受益所有人；
- 企业规模与行业。

## 2. 客户合并与拆分

实际系统中可能出现同一个自然人被建立多个客户号的情况。

客户合并时应考虑：

- 账户归属；
- 风险评级；
- 黑名单；
- 营销标签；
- 历史流水；
- 主客户号保留规则。

客户合并通常不能简单 delete 数据，而应保留映射和历史审计。

## 3. 账户

账户是银行记录资产、负债和内部资金的基本载体。

常见账户类型：

- 活期账户；
- 定期账户；
- 贷款账户；
- 信用卡账户；
- 内部账户；
- 清算账户；
- 暂收暂付账户。

## 4. 账号与账户 ID

推荐区分：

- account_id：系统内部唯一标识；
- account_no：对外或业务使用账号。

原因：

- 账号规则可能变更；
- 不同法人可能存在不同编号规则；
- 内部主键不应被业务编号绑死。

## 5. 账户生命周期

```text
开户 -> 正常 -> 限制/冻结 -> 销户
```

实际状态可能更细：

- 待激活；
- 正常；
- 只收不付；
- 不收不付；
- 冻结；
- 久悬；
- 销户。

交易前必须根据交易类型校验账户状态，而不是只判断“是不是正常”。

## 6. 余额模型

常见字段：

```text
ledger_balance      账面余额
available_balance   可用余额
frozen_amount       冻结金额
overdraft_limit     透支额度
```

不要认为数据库里只有一个 `balance` 就足够。

## 7. 冻结

冻结一般是资金控制记录，不建议仅在账户表上维护一个冻结总额。

建议保留冻结明细：

```text
freeze_id
account_id
amount
reason
source
start_time
expire_time
status
original_txn_id
```

这样才能支持多笔冻结、部分解冻、到期自动解冻和审计。

## 8. 产品与账户

开户时账户通常绑定某个产品。

产品决定：

- 利率；
- 币种；
- 费用；
- 期限；
- 最低余额；
- 透支规则；
- 计息规则；
- 结息周期。

因此核心数据模型常见关系为：

```text
客户 -> 账户 -> 产品
```

## 9. 多币种

不要只在金额字段中存数值，金额必须与币种一起理解。

例如：

```text
amount = 100
currency = CNY
```

跨币种交易还需要：

- 原币金额；
- 目标币金额；
- 汇率；
- 汇率来源；
- 汇兑损益。

## 10. 金额字段设计

金融系统通常使用定点数/Decimal，不应使用 float/double 表示货币。

原因是二进制浮点存在精度问题。

数据库中常见：

```sql
DECIMAL(18,2)
```

但不同币种小数位并不一定相同，因此实际系统要按币种定义处理。

## 11. 账户交易明细

一笔明细通常至少包含：

- 账户；
- 交易流水；
- 业务日期；
- 交易时间；
- 借贷方向；
- 发生额；
- 交易后余额；
- 对手账号；
- 摘要；
- 渠道；
- 原交易流水。

“交易后余额”对于审计、流水展示和问题排查十分有价值。

## 12. 开户与销户

开户需要校验：

- 客户状态；
- 产品是否可售；
- 币种；
- 实名/KYC；
- 数量限制；
- 合规要求。

销户一般要检查：

- 余额是否为零；
- 是否存在冻结；
- 是否存在未完成交易；
- 是否有未结利息/费用；
- 是否有关联协议。

## 13. 开发检查清单

- 客户号和账号是否分离？
- 金额是否使用 Decimal？
- 币种是否随金额保存？
- 冻结是否保留明细？
- 可用余额如何计算？
- 账户状态是否按交易场景判断？
- 销户是否检查未完成业务？
- 数据变更是否可审计？
