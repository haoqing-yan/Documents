# 银行核心运维与容灾

银行核心的运维目标不是“服务能启动”，而是确保关键业务在故障、切换、批量、网络异常和数据恢复场景下仍然满足可用性和账务正确性要求。

## 1. 可用性目标

常见指标：

- SLA：服务可用率；
- RTO：故障后允许多长时间恢复；
- RPO：允许丢失多少时间范围的数据。

对于关键资金交易，RPO 往往要求极低。

## 2. 两地三中心

典型形态：

- 生产中心；
- 同城灾备中心；
- 异地灾备中心。

同城中心主要应对：

- 机房故障；
- 电力故障；
- 局部网络故障。

异地中心主要应对：

- 城市级灾害；
- 大范围基础设施故障；
- 重大数据中心事故。

## 3. 主备与双活

### 主备

主中心对外服务，备中心同步数据并待命。

优势：

- 架构相对简单；
- 数据写入路径清晰。

难点：

- 切换时间；
- 数据同步延迟；
- 回切复杂。

### 双活

两个中心同时提供业务能力。

真正难点不是流量分配，而是：

- 数据写冲突；
- 跨中心事务；
- 网络分区；
- 双主风险；
- 账户路由。

金融核心做双活时通常需要明确数据归属和写入边界。

## 4. 防止双主

灾备切换最危险的场景之一是原中心并未完全失效，两边同时写入。

常见控制：

- 仲裁；
- fencing token；
- 数据库角色控制；
- 网络隔离；
- 强制停止旧主写权限。

## 5. 批量作业

银行核心有大量日终任务：

- 日切；
- 计息；
- 计提；
- 结息；
- 费用；
- 贷款扣款；
- 逾期迁移；
- 总账汇总；
- 对账；
- 报表。

批量系统必须支持：

- 依赖管理；
- 失败重跑；
- 断点续跑；
- 幂等；
- 并行执行；
- 超时告警；
- 人工干预审计。

## 6. 批量重跑

批量程序不能假设“只运行一次”。

例如利息批量重复执行时，如果没有幂等，会出现重复计息。

常见幂等键：

```text
业务日期 + 任务名 + 账户/借据 + 处理类型
```

## 7. 数据库备份与恢复

备份只是第一步，必须定期验证恢复能力。

需要考虑：

- 全量备份；
- 增量备份；
- 日志归档；
- PITR；
- 备份加密；
- 异地保存；
- 定期恢复演练。

## 8. 数据恢复后的账务校验

数据库恢复后不能只看“库启动成功”。

至少需要核查：

- 交易流水连续性；
- 账户余额；
- 分户账；
- 总账；
- MQ 消费位点；
- 外部支付状态；
- 批量任务状态。

必要时进行全量或增量对账。

## 9. 监控

### 技术指标

- CPU；
- 内存；
- GC；
- 数据库连接；
- SQL 延迟；
- MQ 堆积；
- 磁盘；
- 网络。

### 业务指标

更加重要：

- 交易成功率；
- 每秒交易量 TPS；
- 平均响应时间；
- 超时率；
- 冲正率；
- 单边账数量；
- 批量完成进度；
- 对账差异数量。

仅看 CPU 正常并不能说明核心系统正常。

## 10. 日志

日志应支持按以下维度检索：

- traceId；
- 全局流水；
- 核心流水；
- 渠道流水；
- 账号脱敏值；
- 交易码。

严禁把敏感认证数据直接打印到日志。

## 11. 灰度与发布

银行核心发布需要考虑：

- 数据库脚本是否向后兼容；
- 新旧版本是否能同时运行；
- 消息格式是否兼容；
- 是否可快速回滚；
- 正在处理的交易会不会受到影响。

推荐：

- expand/contract 数据库变更；
- API 向后兼容；
- 特性开关；
- 小流量灰度。

## 12. 应急处理原则

遇到资金类故障时：

1. 先阻止影响继续扩大；
2. 保留现场和流水；
3. 判断账务实际状态；
4. 禁止无依据直接改数据库；
5. 通过标准补账、冲正、调账流程修复；
6. 完成对账；
7. 留下完整审计记录。

## 13. 运维检查清单

- 单节点故障是否自动摘除？
- 主库故障如何切换？
- 如何保证不会双主？
- MQ 堆积如何发现？
- 日终失败能否断点续跑？
- 批量任务能否安全重跑？
- 备份是否实际做过恢复演练？
- 容灾切换后如何核账？
- 是否有业务指标监控？
- 资金故障是否有标准应急流程？
