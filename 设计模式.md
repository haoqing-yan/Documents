# 银行核心常见设计模式

银行系统里的设计模式不只指 GoF 模式，更重要的是围绕 **交易一致性、资金安全、可恢复性、可扩展性** 形成的一套工程模式。

## 1. 幂等模式

目标：同一业务请求重复提交时，只产生一次业务效果。

常见实现：

- 唯一业务流水号；
- 数据库唯一约束；
- 幂等表；
- 状态机；
- 原交易结果复用。

适用：

- 扣款；
- 转账；
- 放款；
- 还款；
- 支付回调。

## 2. 状态机模式

用于约束交易生命周期。

例如：

```text
INIT -> PROCESSING -> SUCCESS
                  -> FAILED
                  -> UNKNOWN -> REVERSED
```

价值：

- 禁止非法跳转；
- 明确重试边界；
- 方便故障恢复；
- 支持审计和对账。

## 3. 补偿事务模式

当跨系统无法用单一数据库事务保证一致性时，通过反向动作恢复业务状态。

例如：

```text
A 系统冻结资金成功
B 系统下单失败
=> A 系统执行解冻补偿
```

注意：补偿不是数据库 rollback，它本身也是一笔新的业务操作。

## 4. 冲正模式

用于解决金融交易中的技术性不确定状态。

核心要求：

- 冲正关联原交易；
- 幂等；
- 可重试；
- 能判断原交易是否实际成功；
- 不得造成二次资金变化。

## 5. Outbox 模式

解决“数据库提交成功，但 MQ 消息发送失败”的问题。

```text
业务表 + outbox_event
        同一事务提交
              |
              v
        异步发送 MQ
```

消息发送成功后更新事件状态。

这比“先改数据库，再直接发消息”更加可靠。

## 6. Saga 模式

把长业务拆成多个本地事务：

```text
T1 -> T2 -> T3 -> T4
```

失败后执行：

```text
C3 -> C2 -> C1
```

适合：

- 复杂贷款流程；
- 跨产品业务；
- 长时间业务编排。

## 7. TCC 模式

```text
Try     预留资源
Confirm 正式提交
Cancel  释放资源
```

适合强业务资源预留场景。

难点：

- 空回滚；
- 悬挂；
- 重复 Confirm/Cancel；
- 业务侵入较大。

## 8. 账户串行化模式

对同一账户的资金交易按账户维度串行处理。

好处：

- 降低并发余额竞争；
- 容易保证顺序。

代价：

- 热点账户吞吐受限；
- 队列异常需要恢复。

## 9. 乐观锁模式

增加版本字段：

```sql
update account
set balance = :newBalance,
    version = version + 1
where account_no = :accountNo
  and version = :oldVersion;
```

更新失败后重新读取或重试。

适合冲突概率相对较低的场景。

## 10. 悲观锁模式

```sql
select ... for update
```

适合强一致且冲突较高的关键账户处理，但要关注：

- 锁等待；
- 死锁；
- 长事务。

## 11. CQRS 思想

命令和查询分离：

- 写模型保证交易正确；
- 查询模型支持复杂查询、报表和历史数据。

适合银行大量“交易少、查询多”的外围场景。

但余额查询等需要强一致的数据不能盲目异步化。

## 12. 冷热数据分层

例如流水：

```text
近 3 个月 -> 热库
历史数据 -> 历史库/归档库
```

减少核心在线库压力。

## 13. 防腐层模式

核心系统与外部系统之间增加适配层，避免外部协议和字段污染核心模型。

例如：

```text
外部支付报文
    -> Adapter
    -> 核心统一交易模型
```

## 14. 参数化规则模式

将经常变化的业务规则配置化：

- 利率；
- 费用；
- 限额；
- 产品参数；
- 会计规则；
- 节假日。

但不能把所有逻辑都做成万能配置，否则配置系统会变成另一套编程语言。

## 15. 审计日志模式

对于资金、权限和参数变更，记录：

- 操作人；
- 操作时间；
- 原值；
- 新值；
- 业务流水；
- 来源渠道；
- 审批信息。

审计日志不应允许普通业务代码随意修改。

## 16. 对账模式

对于跨系统资金链路，不能只相信接口调用结果。

通过双方流水定期比对发现：

- 单边账；
- 金额不一致；
- 状态不一致；
- 重复交易。

对账是分布式金融系统最终一致性的最后一道防线。
