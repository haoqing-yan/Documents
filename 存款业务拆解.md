# 银行存款业务拆解

存款核心看起来只是“账户余额”，实际上至少包括 **开户、账户状态、资金收付、冻结、计息、结息、定期生命周期、销户和核算**。

---

# 1. 存款产品分类

```text
存款
├─ 活期存款
├─ 定期存款
├─ 通知存款
├─ 协定存款
├─ 大额存单
└─ 其他特色存款产品
```

系统设计时不要把产品名称直接写进交易代码，而应该由产品参数决定行为。

---

# 2. 活期存款模型

核心对象：

```text
客户
↓
账户
↓
子账户/币种账户
↓
余额
↓
账户明细
```

典型能力：

- 开户；
- 存款；
- 取款；
- 行内转账；
- 查询；
- 冻结/解冻；
- 止付/解止付；
- 计息；
- 结息；
- 销户。

---

# 3. 开户模块

开户不是 insert account。

一般需要：

```text
客户身份校验
↓
产品准入
↓
KYC/风险检查
↓
账户数量限制
↓
生成账号
↓
建立账户属性
↓
建立计息参数
↓
建立会计映射
↓
绑定渠道/介质
↓
开户完成
```

账户至少需要维护：

```text
account_no
customer_id
product_id
currency
branch_id
status
open_date
ledger_balance
available_balance
interest_rule
```

---

# 4. 账号与账户不是一个概念

某些核心模型中：

```text
客户可见账号
    ↓
主账户
    ↓
多个子账户
```

例如多币种账户：

```text
主账号 888888
├─ CNY 子账户
├─ USD 子账户
└─ EUR 子账户
```

因此接口设计中应该区分：

- account number；
- account id；
- sub account id。

---

# 5. 账户状态机

建议显式定义。

```text
PENDING
↓
ACTIVE
├─ FROZEN
├─ STOP_PAYMENT
├─ DORMANT
├─ SUSPENDED
└─ CLOSED
```

不同状态需要定义交易矩阵。

例如：

| 状态 | 入账 | 出账 | 查询 |
|---|---|---|---|
| ACTIVE | 是 | 是 | 是 |
| 只收不付 | 是 | 否 | 是 |
| 冻结 | 视规则 | 否/部分 | 是 |
| 销户 | 否 | 否 | 是 |

状态控制最好集中在 AccountControlService，而不是每个交易重复 if。

---

# 6. 存款模块

现金或转入资金进入账户。

典型链路：

```text
请求
↓
账户状态检查
↓
交易限额
↓
反洗钱/风控检查
↓
记账
↓
余额增加
↓
明细
↓
会计流水
```

现金存款和转账入账在会计对手方上不同。

---

# 7. 取款模块

重点检查：

- 可用余额；
- 冻结金额；
- 账户状态；
- 单笔/单日限额；
- 柜员现金权限；
- 大额授权；
- 密码/身份认证。

核心条件：

```text
available_balance >= withdrawal_amount
```

但允许透支的产品例外。

---

# 8. 冻结模型

不要只在 account 表加一个 frozen_amount。

最好有冻结明细：

```text
freeze_id
account_id
freeze_type
amount
remaining_amount
start_date
end_date
reason
source_system
original_transaction_id
status
```

这样才能支持：

- 多笔冻结；
- 部分解冻；
- 到期自动解冻；
- 司法冻结；
- 支付预授权。

---

# 9. 止付模型

止付和冻结并不完全相同。

冻结通常针对金额：

```text
冻结 1000 元
```

止付可以针对账户行为：

```text
禁止所有出账
```

因此建议账户控制独立建模。

---

# 10. 可用余额引擎

可用余额不是简单数据库字段。

根据产品可能：

```text
Available Balance
=
Ledger Balance
- Frozen
- Payment Hold
- Pending Debit
+ Overdraft Limit
```

最好由统一 BalanceService 计算或维护。

---

# 11. 定期存款模型

核心对象：

```text
定期账户
本金
存期
起息日
到期日
利率
自动转存方式
支取规则
```

状态：

```text
ACTIVE
↓
MATURED
↓
ROLLED_OVER / WITHDRAWN / CLOSED
```

---

# 12. 定期开户

处理：

```text
活期扣款
↓
定期本金入账
↓
确定存期
↓
确定执行利率
↓
确定起息日
↓
计算到期日
↓
设置自动转存
```

必须保证活期扣款和定期创建之间的一致性。

---

# 13. 到期处理

日终发现：

```text
maturity_date = business_date
```

根据指示：

## 本息转活期

```text
本金 + 利息 -> 活期账户
```

## 本金转存、利息转活期

```text
本金 -> 新一期定期
利息 -> 活期
```

## 本息自动转存

```text
本金 + 利息 -> 新一期定期
```

每一种都必须产生清晰的原账户与新账户关系。

---

# 14. 提前支取

提前支取往往需要重新计算利息。

可能：

```text
原定期利率 -> 改按提前支取规则计息
```

如果允许部分提前支取：

```text
原本金
├─ 支取部分
└─ 剩余部分继续定期
```

还要考虑产品规定的部分支取次数。

---

# 15. 计息积数

活期常见积数思想：

```text
每日积数 = 当日计息余额 × 天数
```

累计：

```text
累计积数 = Σ 每日积数
```

结息：

```text
interest = accumulated_balance × daily_rate
```

实际规则要考虑利率分层、日期规则和舍入。

---

# 16. 利率分层

例如产品可以设计：

```text
0 ~ 1 万        利率 A
1 万 ~ 10 万    利率 B
10 万以上       利率 C
```

要明确是：

- 全额按档；
- 分段累进。

两者利息完全不同。

---

# 17. 结息

结息流程：

```text
扫描应结息账户
↓
计算积数
↓
计算利息
↓
生成结息流水
↓
客户账户入账
↓
生成利息支出会计分录
↓
更新累计积数
```

批量重跑必须幂等。

---

# 18. 销户

不能直接 status=CLOSED。

需要检查：

```text
余额是否为零
冻结是否解除
未达账是否完成
签约关系是否解除
应付利息是否结清
费用是否结清
```

然后：

```text
结息
转出余额
销户
```

---

# 19. 睡眠户与久悬户

长期无交易账户可能进入特殊状态。

业务上可能影响：

- 出账权限；
- 柜面激活；
- 费用；
- 客户身份重新核验。

这类状态通常由日终批量识别。

---

# 20. 内部账户

银行内部账户虽然不是客户账户，但同样重要。

例如：

```text
现金内部户
手续费收入户
利息支出户
清算往来户
挂账户
```

内部户通常需要更严格的：

- 科目控制；
- 机构控制；
- 手工记账权限；
- 日终核对。

---

# 21. 存款产品推荐子服务

```text
AccountOpeningService
DepositService
WithdrawalService
TransferService
AccountControlService
FreezeService
BalanceService
InterestService
TermDepositService
SettlementInterestService
AccountClosureService
```

---

# 22. 存款核心关键一致性

必须满足：

```text
账户余额变化
=
账户明细累计变化
```

并且：

```text
存款分户账汇总
=
对应总账科目余额
```

同时冻结也要满足：

```text
account.frozen_amount
=
有效冻结明细之和
```

---

# 23. 存款开发最容易出错的地方

1. 并发扣款超余额；
2. 冻结金额与冻结明细不一致；
3. 交易重试重复入账；
4. 定期到期批量重复执行；
5. 提前支取利息重算错误；
6. 日切后业务日期与机器日期混用；
7. 销户后仍有未完成交易；
8. 分户账与总账不一致。

---

# 24. 存款业务检查清单

设计一个存款交易时至少确认：

- 操作的是哪个账户层级？
- 账户当前状态允许吗？
- 使用账面余额还是可用余额？
- 是否涉及冻结？
- 是否涉及限额？
- 是否涉及手续费？
- 是否产生利息影响？
- 会计对手方是谁？
- 幂等键是什么？
- 是否允许冲正？
- 冲正后余额如何恢复？
- 日切期间如何确定业务日期？
- 日终如何对账？
