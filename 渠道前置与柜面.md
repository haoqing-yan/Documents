# 银行渠道、前置与柜面模块

银行核心通常不直接面对所有终端。手机银行、网银、柜面、ATM、开放银行等渠道会通过前置、ESB、API Gateway 或服务网关进入核心。

---

# 1. 渠道域总体结构

```text
客户/柜员
↓
渠道应用
↓
渠道前置 / API Gateway / ESB
↓
业务服务
↓
核心交易 / 支付 / 信贷
```

渠道层负责“接入”，核心负责“业务事实和账务”。

---

# 2. 常见渠道

```text
柜面
手机银行
个人网银
企业网银
ATM/CDM
POS
客服
开放银行 API
第三方合作渠道
```

每个渠道的认证方式、限额和可用交易不同。

---

# 3. 渠道前置职责

常见能力：

- 协议接入；
- 报文解析；
- 签名验签；
- 字段转换；
- 服务路由；
- 流量控制；
- 超时控制；
- 全局流水；
- 灰度路由；
- 日志和审计。

不建议把真正的余额、会计和产品规则放在前置层。

---

# 4. 报文模型

渠道报文可以抽象成：

```text
Header
├─ requestId
├─ channel
├─ institution
├─ timestamp
├─ transactionCode
└─ version

Body
└─ 业务字段
```

进入内部后应转换为统一领域请求，而不是让核心理解几十种渠道格式。

---

# 5. 全局流水

建议区分：

```text
channel_seq       渠道流水
request_id        请求幂等号
global_seq        全局追踪号
core_seq          核心流水
external_seq      外部支付流水
```

它们可以关联，但用途不同。

---

# 6. 柜面系统

柜面除了普通交易，还具有银行特有控制。

子模块：

```text
柜员
机构
尾箱
现金
凭证
授权
复核
日结
```

---

# 7. 柜员模型

需要管理：

- 柜员号；
- 所属机构；
- 岗位；
- 交易权限；
- 授权级别；
- 状态；
- 登录终端。

不能只依赖普通 RBAC，因为一些交易还需要金额级授权。

---

# 8. 柜员授权

例如：

```text
5 万以下柜员自行办理
5~50 万主管授权
50 万以上更高级别授权
```

授权记录必须关联：

```text
原交易流水
申请柜员
授权柜员
授权时间
授权原因
```

---

# 9. 尾箱与现金

柜员现金业务要维护尾箱。

```text
期初现金
+ 收入现金
- 支出现金
= 期末现金
```

日终需要账实核对。

尾箱和客户账户是两个不同账务对象。

---

# 10. 凭证管理

支票、存单等重要凭证可能需要：

```text
入库
领用
使用
作废
上缴
销毁
```

每张重要凭证应有状态流转和责任人。

---

# 11. 柜面抹账

柜面错误交易可能允许抹账，但必须受限：

- 原交易必须满足可抹条件；
- 可能仅限当日；
- 需要授权；
- 不能删除原流水；
- 生成反向账务；
- 完整审计。

---

# 12. 手机银行 / 网银

重点模块：

```text
登录认证
设备管理
交易认证
收款人管理
转账
签约
限额
通知
交易查询
```

核心只接收已经完成渠道侧认证后的业务请求，但仍需要做账户、额度和风险检查。

---

# 13. 企业网银

对公渠道经常需要：

```text
制单
复核
授权
发送
```

典型流程：

```text
操作员 A 制单
↓
操作员 B 复核
↓
管理员授权
↓
银行执行
```

这属于客户侧审批流，与银行内部柜员授权要区分。

---

# 14. ATM / 自助设备

典型场景：

- 取款；
- 存款；
- 查询；
- 转账。

ATM 特别需要考虑：

```text
核心扣账成功
但机器没有吐钞
```

所以超时、冲正和设备对账非常关键。

---

# 15. 开放银行 API

常见能力：

- App/商户身份；
- API Key；
- OAuth；
- 签名；
- 时间戳；
- nonce；
- 幂等键；
- 限流；
- 回调。

对外 API 必须进行版本管理。

---

# 16. 渠道限流

限流维度：

```text
渠道
客户
商户
接口
IP
设备
```

技术限流和业务限额不是一回事：

```text
QPS 限流 != 转账金额限额
```

---

# 17. 超时控制

渠道前置应区分：

```text
连接超时
读取超时
业务超时
```

业务超时后不能擅自认为核心失败，应查询原交易。

---

# 18. 服务编排边界

ESB 可以做：

```text
查询客户
+
查询账户
+
组装返回
```

但核心资金交易不建议拆成前置层多个无事务调用：

```text
先扣 A
再加 B
```

资金一致性应该由核心交易服务负责。

---

# 19. 推荐服务

```text
ChannelGateway
MessageAdapter
AuthenticationService
TellerService
AuthorizationService
CashBoxService
VoucherService
ApiAccessService
ChannelLimitService
```

---

# 20. 渠道开发检查清单

- 渠道流水和核心流水是否区分？
- 报文是否统一转换？
- 是否支持版本兼容？
- 重复请求如何幂等？
- 超时后如何查原交易？
- 柜员是否需要授权？
- 企业网银是否有制单复核？
- ATM 吐钞失败如何对账？
- API 是否有限流和验签？
- ESB 是否侵入了核心账务逻辑？
