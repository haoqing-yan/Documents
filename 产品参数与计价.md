# 银行产品参数与计价模型

银行核心系统如果把利率、手续费、期限、限额等全部写死在代码里，最终一定会非常难维护。

所以成熟核心通常会有一套 **产品工厂 + 参数中心 + 计价引擎**。

---

# 1. 产品层级

一个常见抽象：

```text
产品大类 Product Category
   ↓
产品 Product
   ↓
产品版本 Product Version
   ↓
产品实例 / 客户签约
```

例如：

```text
存款
├─ 活期产品 A
├─ 定期产品 B
└─ 大额存单 C
```

贷款也类似：

```text
消费贷
├─ 信用消费贷
├─ 抵押消费贷
└─ 场景消费贷
```

---

# 2. 产品参数分类

## 基础参数

- 币种；
- 最低金额；
- 最高金额；
- 期限；
- 开户方式；
- 是否可透支；
- 是否允许部分支取。

## 利率参数

- 基准利率；
- 加减点；
- 分层利率；
- 罚息利率；
- 重定价周期。

## 费用参数

- 固定费用；
- 按比例费用；
- 阶梯收费；
- 封顶；
- 保底；
- 减免。

## 交易参数

- 单笔限额；
- 单日限额；
- 可用渠道；
- 允许交易类型。

## 核算参数

- 会计事件；
- 科目映射；
- 收入/费用科目；
- 清算科目。

---

# 3. 参数版本化

参数不能直接覆盖。

例如 10 月 1 日修改利率，应保留：

```text
version_1
有效期：2026-01-01 ~ 2026-09-30

version_2
有效期：2026-10-01 ~ ...
```

否则历史交易无法还原。

重要字段：

```text
version
valid_from
valid_to
status
approved_by
```

---

# 4. 生效日模型

参数修改通常经历：

```text
DRAFT
↓
APPROVED
↓
WAITING_EFFECTIVE
↓
ACTIVE
↓
EXPIRED
```

新版本可以提前配置，到指定业务日由日初批量激活。

---

# 5. 利率模型

## 固定利率

整个合同期保持不变。

## 浮动利率

```text
执行利率 = 基准利率 + spread
```

## 分层利率

按余额或金额档位选择。

## 分段累进利率

不同区间分别计算。

---

# 6. 计息日数规则

参数需要明确：

```text
Actual/365
Actual/360
30/360
```

以及：

- 起息日是否计息；
- 到期日是否计息；
- 节假日处理；
- 闰年处理。

不能只配置一个 annual_rate。

---

# 7. 舍入模型

金融系统必须明确：

```text
计算精度
入账精度
舍入方式
```

例如：

- 四舍五入；
- 截断；
- 向上取整。

利息内部计算可能保留更多位，最终入账才保留币种最小单位。

---

# 8. 手续费模型

手续费常见类型：

## 固定金额

```text
fee = 10
```

## 比例

```text
fee = amount × 0.1%
```

## 保底封顶

```text
fee = max(min_fee, min(amount × rate, max_fee))
```

## 阶梯

不同金额区间费率不同。

---

# 9. 优惠模型

客户实际费用可能来自：

```text
标准价格
- 客户等级优惠
- 渠道优惠
- 活动优惠
- 协议优惠
```

需要明确优惠叠加规则。

---

# 10. 客户级定价

尤其对公业务可能存在协议价格：

```text
产品标准利率 3.5%
客户协议利率 3.2%
```

优先级可以设计为：

```text
客户合同价格
> 客户组价格
> 机构价格
> 产品标准价格
```

但必须统一定义，避免不同系统口径不一致。

---

# 11. 计价引擎

推荐独立：

```text
PricingEngine
```

输入：

```text
customer
product
transaction
amount
channel
business_date
```

输出：

```text
interest_rule
fee_rule
discount
final_price
```

---

# 12. 参数与代码边界

适合参数化：

- 利率；
- 费率；
- 限额；
- 期限；
- 还款顺序；
- 会计映射；
- 生效日。

不适合把所有复杂逻辑都做成万能配置平台。

原则：

> 稳定逻辑写代码，易变规则做参数。

---

# 13. 参数缓存

产品参数读取频率高，常进入缓存。

但必须解决：

- 版本切换；
- 缓存失效；
- 多节点一致性；
- 生效日切换。

一个常见模式：

```text
数据库为真源
↓
发布参数版本
↓
缓存预热
↓
业务日期生效
```

---

# 14. 核算映射参数

不要在代码里写：

```java
if (product == "A") subject = "201001";
```

应该配置：

```text
accounting_event
+ product
+ currency
+ institution
-> subject / internal account
```

这样产品扩展不会侵入交易代码。

---

# 15. 产品变更检查清单

每次新产品上线至少确认：

- 客户准入；
- 开户条件；
- 金额上下限；
- 利率；
- 计息规则；
- 手续费；
- 交易限额；
- 账户状态；
- 到期规则；
- 会计分录；
- 冲正规则；
- 日终任务；
- 对账；
- 报表和监管数据。
