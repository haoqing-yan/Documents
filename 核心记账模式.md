# 银行核心记账模式

银行核心系统真正的核心，不是“转账接口”，而是把每一个业务事件稳定地转换成 **账户余额变化、账务明细、会计分录和总账结果**。

可以把核心记账抽象为：

```text
业务事件
  ↓
交易校验
  ↓
账务事件 Accounting Event
  ↓
会计规则 Accounting Rule
  ↓
分录 Journal Entry
  ↓
分户账 / 明细账 / 余额
  ↓
总账 General Ledger
  ↓
对账 / 勾稽 / 报表
```

> 不同银行和核心厂商实现方式不同，但“业务事件和账务处理解耦、借贷平衡、余额可追溯、总分账可勾稽”是最重要的共性。

---

# 1. 核心记账需要解决什么问题

任何一笔资金业务都至少要回答：

1. 谁的钱发生变化？
2. 变化多少？
3. 为什么变化？
4. 借方和贷方分别是谁？
5. 余额是多少？
6. 什么时候生效？
7. 属于哪个会计日期？
8. 是否已经记过账？
9. 如果失败如何恢复？
10. 总账和分户账是否一致？

所以一条“转账成功”的记录远远不够。

---

# 2. 账务四层模型

## 2.1 交易流水

描述一次业务请求。

常见字段：

```text
transaction_id
request_id
channel_seq
core_seq
customer_id
account_no
transaction_code
amount
currency
business_date
transaction_time
status
original_transaction_id
```

交易流水回答的是：

> 发生了一笔什么业务？

## 2.2 账户明细账

记录某个账户发生了哪些资金变化。

```text
account_no
entry_seq
transaction_id
direction
amount
balance_before
balance_after
value_date
business_date
```

回答：

> 这个账户为什么从 1000 变成 900？

## 2.3 分户账

维护每个账户、内部户、产品户的账务余额。

例如：

- 客户活期账户；
- 贷款本金账户；
- 应收利息账户；
- 手续费收入内部户；
- 清算往来账户。

## 2.4 总账

按会计科目和机构等维度汇总。

```text
科目 + 机构 + 币种 + 会计日期
    -> 借方发生额
    -> 贷方发生额
    -> 余额
```

总账回答：

> 银行在会计意义上有多少资产、负债、收入、费用？

---

# 3. 借贷复式记账

银行核心通常遵守复式记账：

```text
借方金额合计 = 贷方金额合计
```

从系统角度可以理解为：

```text
一个业务事件
   ↓
产生 N 条借方分录
产生 M 条贷方分录
   ↓
借贷平衡后才能入账
```

不能把“余额 -100”当成完整的会计处理。

---

# 4. 负债类账户为什么借减贷增

客户存款从银行角度属于银行负债。

例如客户 A 有 1000 元存款，本质上是：

> 银行欠客户 A 1000 元。

因此客户取走 100 元，银行负债减少：

```text
借：客户存款 100
贷：现金 / 清算资金 100
```

理解这一点后，很多银行分录会更容易看懂。

---

# 5. 记账引擎模型

建议把核心记账拆成三个层次。

## 5.1 业务层

负责：

- 参数检查；
- 产品规则；
- 权限；
- 限额；
- 状态判断；
- 业务对象变更。

例如：

```text
transfer()
repayment()
disbursement()
withdraw()
deposit()
```

## 5.2 账务事件层

业务层不要直接写死科目。

业务完成后发送一个语义化的账务事件，例如：

```text
ACCOUNT_TRANSFER
LOAN_DISBURSEMENT
LOAN_REPAYMENT
INTEREST_ACCRUAL
FEE_CHARGE
PAYMENT_CLEARING
```

## 5.3 会计规则层

根据：

```text
事件类型
+ 产品
+ 币种
+ 法人
+ 机构
+ 交易场景
```

匹配会计模板。

例如：

```text
LOAN_REPAYMENT
    借：客户存款
    贷：贷款本金
    贷：应收利息
```

金额通过公式计算，而不是把所有金额写死。

---

# 6. 典型记账方式

银行系统常见的不止一种“记账模式”。

## 6.1 实时逐笔记账

联机交易完成时立即：

```text
更新账户余额
+ 写明细
+ 生成分录
```

适用于：

- 存取款；
- 行内转账；
- 实时支付；
- 贷款还款。

优点：实时性强。

缺点：核心写压力大。

## 6.2 实时分户 + 异步总账

常见设计：

```text
联机交易
    ↓
实时更新分户账和余额
    ↓
生成会计流水
    ↓
MQ / 会计接口
    ↓
异步更新总账
```

这种模式下必须保证：

- 会计事件不丢；
- 可以重放；
- 重放不能重复记账；
- 日终可以检查总分不平。

## 6.3 批量汇总记账

部分业务不是逐笔推总账，而是按：

```text
交易类型 + 科目 + 机构 + 币种
```

汇总后推总账。

典型场景：

- 批量计提；
- 结息；
- 手续费；
- 某些清算业务。

## 6.4 备忘记账

有些信息影响业务控制，但暂不形成正式会计分录。

例如：

- 额度占用；
- 担保信息；
- 某些冻结控制；
- 表外业务。

不能把“业务余额”与“会计余额”混为一谈。

---

# 7. 余额模型

## 7.1 账面余额

正式账务余额。

```text
ledger_balance
```

## 7.2 可用余额

用户实际可使用金额。

```text
available_balance
```

简化公式：

```text
可用余额
= 账面余额
- 冻结金额
- 止付金额
- 在途占用
+ 可用透支额度
```

具体由产品决定。

## 7.3 冻结余额

冻结通常不直接减少账面余额，但是降低可用余额。

因此：

```text
冻结 != 扣款
```

## 7.4 透支余额

对于允许透支的产品还需要维护：

- 授信额度；
- 已用额度；
- 可用额度；
- 透支本金。

---

# 8. 双余额模式

传统核心常见：

```text
当前余额 current_balance
上日余额 previous_balance
最后更新日期 last_update_date
```

目的之一是在日切跨日场景下正确计算余额和积数。

简化逻辑：

```text
交易日期 > 最后更新日期
    上日余额 = 当前余额
    当前余额 += 交易金额
    最后更新日期 = 交易日期

交易日期 = 最后更新日期
    当前余额 += 交易金额

交易日期 < 最后更新日期
    根据历史账务规则调整相关历史余额/积数
```

现代系统可能使用不同的历史余额模型，但核心目标一样：

> 允许系统正确处理业务日期、价值日期和历史账务。

---

# 9. 三种日期

银行账务特别容易因为日期概念混乱出错。

## 9.1 交易日期 Transaction Date

请求实际发生日期。

## 9.2 会计日期 Accounting / Business Date

记入哪个会计日。

日切期间尤其重要。

## 9.3 起息日 Value Date

影响计息的日期。

例如一笔交易可能：

```text
交易日期：09-11
会计日期：09-12
起息日：09-11
```

三者不一定一致。

---

# 10. 中间户与清算户

跨系统、跨机构、跨行交易通常不能简单：

```text
A -> B
```

实际可能需要中间账户：

```text
客户 A
  ↓
支付清算往来户
  ↓
外部清算系统
  ↓
对手行
```

中间户的作用包括：

- 表示在途资金；
- 隔离系统边界；
- 支持对账；
- 支持差错挂账；
- 支持清算资金核对。

---

# 11. 挂账户 Suspense Account

如果资金已经发生，但业务归属暂时无法确认，常见做法不是删除交易，而是进入挂账户。

例如：

```text
外部扣款成功
内部入账失败
```

可以先记录：

```text
清算资金 -> 待处理挂账户
```

后续差错处理再转入正确账户。

挂账必须：

- 有来源流水；
- 有挂账原因；
- 有责任系统；
- 有账龄；
- 可追踪后续销账。

---

# 12. 行内转账记账示例

客户 A 给客户 B 转账 100 元：

```text
借：A 客户存款 100
贷：B 客户存款 100
```

系统层面：

```text
A.balance -= 100
B.balance += 100

insert A_account_detail
insert B_account_detail
insert debit_journal
insert credit_journal
```

要求全部位于一个清晰的事务边界内。

---

# 13. 跨行转账记账示例

本行客户向他行客户汇款 100 元。

核心内部可以简化为：

```text
借：客户存款             100
贷：支付清算往来         100
```

清算完成后再处理清算账户。

这里最重要的是：

> 客户账务状态和外部支付状态是两个状态机。

不能因为外部接口超时就直接把内部状态当失败。

---

# 14. 手续费记账

转账金额 100 元，手续费 2 元：

```text
借：客户存款             102
贷：支付清算往来         100
贷：手续费收入             2
```

系统必须校验：

```text
102 = 100 + 2
```

---

# 15. 贷款放款记账

银行发放贷款 100000 元到客户结算账户。

简化会计含义：

```text
借：贷款本金资产         100000
贷：客户存款负债         100000
```

业务对象同步变化：

```text
借据本金 = 100000
贷款余额 = 100000
客户存款 += 100000
授信可用额度 -= 100000
```

这里至少存在：

- 信贷业务状态；
- 授信额度状态；
- 贷款账务；
- 存款账务。

必须定义清晰的一致性边界。

---

# 16. 贷款还款记账

客户还本金 1000、利息 100：

```text
借：客户存款            1100
贷：贷款本金             1000
贷：应收利息              100
```

同时：

```text
贷款余额 -= 1000
本期应收利息 -= 100
还款计划更新
```

如果只更新“还款计划”而漏记会计账，就是严重账实不符。

---

# 17. 利息计提

贷款每天产生利息，但客户可能月底才支付。

因此日终可以：

```text
借：应收利息
贷：利息收入
```

这叫计提。

实际客户支付时：

```text
借：客户存款
贷：应收利息
```

所以必须区分：

- 已计提；
- 已到期；
- 已收取；
- 未收取。

---

# 18. 冲正模式

不能通过直接删除原流水恢复资金。

正确思想：

```text
原交易：
借 A
贷 B

冲正交易：
借 B
贷 A
```

原交易和冲正交易都保留。

这样审计才能知道：

```text
发生过什么
为什么撤回
谁执行的
```

---

# 19. 红字 / 反向分录思想

不同系统实现术语不同，但基本原则是：

> 已经正式入账的数据通常通过反向账务纠正，而不是物理删除。

尤其跨日、跨月、跨年后，更需要考虑：

- 会计期间；
- 损益；
- 报表；
- 历史余额；
- 监管数据。

---

# 20. 幂等记账

记账接口必须天然考虑重试。

一种常见唯一键：

```text
source_system
+ original_request_id
+ accounting_event_type
+ accounting_sequence
```

如果同一事件第二次到达：

```text
发现已成功
-> 返回原结果
-> 不再次更新余额
```

绝不能靠“调用方保证只调用一次”。

---

# 21. 账户并发模型

高并发账户需要避免丢失更新。

## 悲观锁

```sql
select balance
from account
where account_no = ?
for update;
```

优点：逻辑直接。

缺点：热点账户容易锁竞争。

## 乐观锁

```text
balance
version
```

更新：

```sql
update account
set balance = ?, version = version + 1
where account_no = ?
  and version = ?;
```

失败后重试。

## 单账户串行化

按账户号路由到同一处理分区：

```text
hash(account_no) -> partition
```

同账户顺序处理，不同账户并行。

适用于非常高并发的账务架构。

---

# 22. 余额和流水必须同时成功吗

原则上：

```text
余额变化
+ 账户明细
+ 核心记账流水
```

应处于非常强的一致性边界。

否则会出现最危险的情况：

```text
余额扣了
但是找不到流水
```

总账可以根据架构异步，但必须保证可靠投递和可补偿。

---

# 23. 推荐的核心账务表模型

仅作概念示例。

## account

```text
account_id
customer_id
product_id
currency
ledger_balance
available_balance
frozen_amount
status
version
last_business_date
```

## transaction

```text
transaction_id
request_id
transaction_code
business_date
amount
status
original_transaction_id
```

## account_entry

```text
entry_id
transaction_id
account_id
direction
amount
balance_before
balance_after
value_date
```

## accounting_journal

```text
journal_id
transaction_id
accounting_event
subject_code
internal_account
debit_credit
amount
currency
institution
business_date
```

## accounting_event_outbox

```text
id
transaction_id
event_type
payload
status
retry_count
```

用于可靠地向总账或下游发送会计事件。

---

# 24. 核心账务不变量

银行账务程序最好显式定义不变量。

## 不变量 1：借贷平衡

```text
Σ debit = Σ credit
```

## 不变量 2：余额可还原

```text
期初余额 + 所有有效发生额 = 当前余额
```

## 不变量 3：幂等

```text
同一业务请求只能产生一次有效资金变化
```

## 不变量 4：冲正可追溯

```text
reversal.original_transaction_id != null
```

## 不变量 5：总分账可勾稽

```text
总账科目余额 = 对应分户余额汇总
```

## 不变量 6：不能存在无来源资金变化

每一条账户明细必须能够找到：

```text
交易流水 / 批量流水 / 调账凭证
```

---

# 25. 对账体系

银行核心不是依赖“代码永远不出错”，而是还依赖对账发现错误。

常见：

## 账账核对

```text
分户账 vs 总账
```

## 账实核对

```text
核心账务 vs 实际清算结果
```

## 系统间核对

```text
核心 vs 支付
核心 vs 信贷
核心 vs 卡系统
```

## 流水核对

```text
请求流水 vs 会计流水
```

发现差异后进入：

```text
自动补偿
人工差错平台
挂账
调账
```

---

# 26. 面向 Java 开发的推荐分层

```text
TransferApplicationService
        ↓
TransferDomainService
        ↓
AccountingService
        ↓
AccountingRuleEngine
        ↓
PostingEngine
        ↓
AccountRepository / JournalRepository
```

避免：

```java
transfer() {
    updateBalance();
    insertJournal();
    if (...) {
        // 到处直接写科目
    }
}
```

核心账务应该逐渐形成一个独立、稳定的 Posting Engine。

---

# 27. 记账引擎开发检查清单

每设计一个资金类交易，都检查：

- 业务事件是什么？
- 借方是谁？
- 贷方是谁？
- 是否可能多借多贷？
- 借贷是否平衡？
- 更新哪些账户余额？
- 写哪些账户明细？
- 是否经过中间户？
- 是否有手续费？
- 是否有冻结或解冻？
- 会计日期是什么？
- 起息日是什么？
- 是否允许跨日处理？
- 幂等键是什么？
- 事务边界在哪里？
- 超时如何确定最终状态？
- 冲正怎么生成？
- 总账实时还是异步？
- 异步事件丢失如何补？
- 日终如何勾稽？

如果这些问题都能明确回答，一笔银行交易的账务设计才算基本完整。
