# 支付与清算

支付负责“把钱从一个主体转移到另一个主体”，清算负责确认参与方之间最终应收应付关系，结算则完成资金最终划拨。

## 1. 基本概念

常见链路：

```text
客户 -> 渠道 -> 银行支付前置 -> 核心账务 -> 清算网络/他行
```

一笔跨行支付通常会经历：

- 受理；
- 鉴权；
- 扣账/冻结；
- 报文发送；
- 对方受理；
- 清算；
- 最终结算；
- 对账。

## 2. 行内转账与跨行转账

### 行内转账

收付款账户都在本行，通常可以在核心内部完成双边记账。

### 跨行转账

需要通过清算网络或支付系统与他行交互，因此存在：

- 网络超时；
- 状态不确定；
- 对方拒绝；
- 退汇；
- 清算差错。

所以跨行支付更依赖状态机、冲正、退汇和对账。

## 3. 支付状态

推荐状态不要只有成功/失败：

```text
INIT
-> ACCEPTED
-> DEBITED
-> SENT
-> PROCESSING
-> SUCCESS
-> FAILED
-> RETURNED
-> REVERSED
```

不同系统命名可能不同，但一定要能表达“处理中/未知”。

## 4. 扣账与冻结

有两种常见模型：

### 直接扣账

本行确认受理后立即减少客户余额。

### 先冻结后扣账

先占用资金，外部处理成功后正式扣账，失败则解冻。

选择哪种方式取决于业务和支付链路设计。

## 5. 支付幂等

支付场景必须有唯一请求标识。

典型键：

```text
merchant/orderNo
channel/channelSeq
paymentRequestId
```

对于同一个支付请求：

- 不能重复扣款；
- 不能重复入账；
- 回调可重复，但处理必须幂等。

## 6. 退汇与退款

需要区分：

- 原路退款：基于原支付发起返还；
- 退汇：收款侧无法入账，将资金退回；
- 撤销：支付尚未最终完成时取消；
- 冲正：技术故障导致的补偿。

它们的业务语义和账务处理不同。

## 7. 清算

清算用于计算参与机构之间净应收应付。

例如：

```text
银行 A 当日应付银行 B：1000 万
银行 B 当日应付银行 A：800 万
```

净额清算后：

```text
银行 A 净付银行 B：200 万
```

也可能采用逐笔实时结算，具体取决于支付系统规则。

## 8. 对账

支付系统对账通常至少包含：

- 本行核心流水；
- 支付前置流水；
- 清算机构流水；
- 对手行结果。

常见差异：

| 我方 | 对方 | 处理 |
|---|---|---|
| 成功 | 成功 | 正常 |
| 成功 | 无记录 | 查证/冲正/补单 |
| 无记录 | 成功 | 补账/人工核查 |
| 金额不同 | 金额不同 | 高优先级差错 |

## 9. 支付通知

通知不是支付最终状态的唯一依据。

原因：

- 回调可能丢失；
- 回调可能重复；
- 回调可能乱序。

因此调用方应支持主动查询支付状态。

## 10. 对公批量支付

企业批量付款场景还应处理：

- 批次号；
- 明细号；
- 部分成功；
- 总金额校验；
- 审批；
- 单笔与批次状态聚合。

例如 100 笔付款中 98 笔成功、2 笔失败，不能简单把整个批次标成“失败”。

## 11. 限额与风控

支付前常见校验：

- 单笔限额；
- 单日累计限额；
- 渠道限额；
- 客户等级限额；
- 账户状态；
- 黑名单；
- 反欺诈规则。

限额校验本身也要考虑并发，否则多个请求可能同时穿透日累计限额。

## 12. 开发检查清单

- 支付请求是否全局唯一？
- 重复回调是否安全？
- 外部超时后状态如何确认？
- 扣账后外部失败怎么办？
- 是否支持退汇/退款/冲正？
- 是否有日终对账？
- 是否允许部分成功？
- 是否存在乱序消息？
- 清算日与交易日是否一致？
