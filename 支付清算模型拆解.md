# 支付清算模型拆解

支付系统与核心记账最大的区别是：**核心关心本行账户和账务是否正确，支付系统还要处理外部参与方、网络不确定性、在途资金和最终清算。**

---

# 1. 支付领域对象

建议至少区分：

```text
PaymentOrder        支付订单
PaymentInstruction  支付指令
PaymentTransaction  支付交易
ClearingRecord      清算记录
SettlementRecord    结算记录
ReconciliationItem  对账记录
ReturnOrder         退汇/退款
```

不要把支付订单、核心交易流水和清算流水混成一个 ID。

---

# 2. 支付订单

描述客户要完成什么业务：

```text
payment_order_id
payer
payee
amount
currency
channel
purpose
status
```

一个支付订单可能对应多个技术动作。

例如跨行汇款：

```text
支付订单
├─ 本行扣账
├─ 对外发送
├─ 清算
└─ 最终结算
```

---

# 3. 支付指令

支付指令是提交给下游系统或清算网络的正式消息。

常见字段：

```text
instruction_id
payment_order_id
source_system
external_reference
message_type
send_status
response_code
```

支付订单成功不等于每个指令都只执行一次，因此指令层也需要幂等。

---

# 4. 支付状态机

推荐把内部和外部状态分开。

业务状态示例：

```text
INIT
↓
ACCEPTED
↓
PROCESSING
↓
SUCCESS / FAILED / RETURNED
```

资金状态示例：

```text
NOT_DEBITED
↓
FROZEN / DEBITED
↓
CLEARING
↓
SETTLED
```

外部状态示例：

```text
NOT_SENT
SENT
ACCEPTED
REJECTED
UNKNOWN
```

这样可以避免一个 status 字段承担所有语义。

---

# 5. 行内支付

链路相对简单：

```text
付款账户检查
↓
付款账户扣账
↓
收款账户入账
↓
生成双方明细
↓
生成会计分录
```

通常可以在核心强事务内完成。

---

# 6. 跨行往账

典型：

```text
客户发起
↓
核心扣账/冻结
↓
支付前置
↓
清算网络
↓
对手行
↓
状态回执
↓
清算/结算
```

关键问题不是正常路径，而是：

```text
钱已经扣了，但外部到底成功没有？
```

---

# 7. 跨行来账

来账一般从外部开始：

```text
清算网络来账
↓
支付前置验签/去重
↓
收款账户检查
↓
核心入账
↓
返回处理结果
```

如果收款账户不存在、销户或受限，需要进入退汇或差错流程。

---

# 8. 扣账模式

## 直接扣账

```text
付款账户余额直接减少
```

外部失败后通过退款/冲正恢复。

## 冻结后扣账

```text
冻结资金
↓
外部成功
↓
冻结转正式扣账
```

外部失败：

```text
解冻
```

二者没有绝对好坏，要根据支付链路、时效和会计模型决定。

---

# 9. 在途资金

跨行支付常存在资金已离开客户账户，但尚未最终结算的阶段。

因此需要清算往来户或在途科目：

```text
客户存款
↓
支付清算往来
↓
外部清算
```

在途资金必须能按流水、日期和清算渠道追踪。

---

# 10. 清分、清算、结算

## 清分

识别每个参与方的应收应付。

## 清算

计算最终应收应付关系。

可以是：

```text
逐笔
或
净额
```

## 结算

真正完成参与机构之间资金划拨。

因此：

```text
支付成功
!=
银行间最终结算完成
```

---

# 11. 实时全额结算与净额结算

## RTGS 思想

每笔逐笔最终结算。

优点：结算风险低。

缺点：流动性占用更高。

## 净额思想

一定周期内先累计：

```text
应收 - 应付 = 净头寸
```

再进行结算。

对系统而言需要维护批次和头寸。

---

# 12. 支付幂等

至少有三个层次：

```text
客户请求幂等
核心扣账幂等
对外指令幂等
```

不要认为 payment_order_id 一个字段就能解决所有问题。

---

# 13. 超时与 UNKNOWN

外部请求超时只能说明：

```text
没有收到响应
```

不能说明：

```text
对方没有处理
```

因此必须进入：

```text
UNKNOWN
↓
主动查询
↓
SUCCESS / FAILED
```

长期无法确认才进入人工差错流程。

---

# 14. 退款、退汇、撤销、冲正

## 退款 Refund

原支付已经成功，之后按业务要求返还资金。

## 退汇 Return

收款侧无法完成入账，资金沿支付链路退回。

## 撤销 Cancel

原业务尚未完成时取消。

## 冲正 Reversal

技术异常导致原交易状态不确定或部分成功时的补偿。

四者不能共用一个“退款接口”。

---

# 15. 支付回调

回调必须考虑：

- 重复；
- 乱序；
- 丢失；
- 延迟。

正确做法：

```text
回调推动状态
+
主动查询兜底
+
日终对账最终确认
```

---

# 16. 批量支付

一个批次包含 N 笔明细：

```text
batch_order
├─ payment_001 SUCCESS
├─ payment_002 SUCCESS
├─ payment_003 FAILED
└─ payment_004 PROCESSING
```

批次状态需要聚合，而不是强迫所有明细同状态。

---

# 17. 支付限额

建议维护：

```text
单笔限额
日累计
月累计
渠道限额
客户限额
账户限额
商户限额
```

累计限额检查要具备并发一致性，否则多个请求可同时穿透。

---

# 18. 支付对账

匹配键可以包括：

```text
我方流水
外部流水
金额
币种
交易日期
```

结果：

```text
MATCHED
LOCAL_ONLY
REMOTE_ONLY
AMOUNT_MISMATCH
STATUS_MISMATCH
```

---

# 19. 差错处理

差错平台至少支持：

```text
查询原交易
查询外部状态
补单
冲正
退款
挂账
调账申请
人工确认
```

所有操作都必须留痕。

---

# 20. 支付服务拆分建议

```text
PaymentOrderService
PaymentRoutingService
PaymentInstructionService
PaymentAccountingService
ClearingService
SettlementService
ReturnService
ReconciliationService
PaymentExceptionService
```

---

# 21. 支付一致性检查

成功支付至少应满足：

```text
支付订单状态
核心扣账状态
支付指令状态
清算状态
```

之间能够解释。

不要求所有时刻完全同步，但必须存在合法的状态迁移和最终一致路径。

---

# 22. 支付开发检查清单

- 订单和交易流水是否分离？
- 客户请求是否幂等？
- 外部指令是否幂等？
- 外部超时如何查询？
- 钱在处理中时记在哪？
- 是否有在途账户？
- 退款、退汇、撤销、冲正是否分开？
- 回调重复和乱序是否安全？
- 批量是否支持部分成功？
- 清算日和交易日是否区分？
- 对账发现单边账后如何处理？
